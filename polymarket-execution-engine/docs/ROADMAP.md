# Execution engine roadmap

> Status: current v0.23.0 source-candidate documentation. Historical gate-specific notes are archived under `docs/archive/`; current validation entrypoint is `validation/run_current_gates.sh`.

## Current phase

Server-authoritative execution service hardening.

## Next

1. Run v0.15 gates.
2. Implement PostgreSQL runtime provider.
3. Extend object graph negative tests.
4. Persist admin failure audit where principal is known.
5. Add no-live-submit adapter matrix.
6. Only then continue toward shadow-mode submit design.
